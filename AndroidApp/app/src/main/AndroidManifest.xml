<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:name=".HospitalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmedicKApp"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Splash -->
        <activity
            android:name="com.amedick.hospitalapp.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AmedicKApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name="com.amedick.hospitalapp.activities.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Register -->
        <activity
            android:name="com.amedick.hospitalapp.activities.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Main (home) -->
        <activity
            android:name="com.amedick.hospitalapp.activities.MainActivity"
            android:exported="false" />

        <!-- Doctor Details -->
        <activity
            android:name="com.amedick.hospitalapp.activities.DoctorDetailsActivity"
            android:exported="false"
            android:parentActivityName="com.amedick.hospitalapp.activities.MainActivity" />

        <!-- Book Appointment -->
        <activity
            android:name="com.amedick.hospitalapp.activities.BookAppointmentActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName="com.amedick.hospitalapp.activities.DoctorDetailsActivity" />

        <!-- Doctor Dashboard -->
        <activity
            android:name="com.amedick.hospitalapp.activities.DoctorDashboardActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Doctor Availability -->
        <activity
            android:name="com.amedick.hospitalapp.activities.DoctorAvailabilityActivity"
            android:exported="false"
            android:parentActivityName="com.amedick.hospitalapp.activities.DoctorDashboardActivity" />

        <!-- Chat Activity -->
        <activity
            android:name="com.amedick.hospitalapp.activities.ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Review Activity -->
        <activity
            android:name="com.amedick.hospitalapp.activities.ReviewActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Admin Dashboard -->
        <activity
            android:name="com.amedick.hospitalapp.activities.AdminDashboardActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Admin Verification Activity -->
        <activity
            android:name="com.amedick.hospitalapp.activities.AdminVerificationActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Medical Profile Activity -->
        <activity
            android:name="com.amedick.hospitalapp.activities.MedicalProfileActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Medical Documents Activity -->
        <activity
            android:name="com.amedick.hospitalapp.activities.MedicalDocumentsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Notifications Activity -->
        <activity
            android:name="com.amedick.hospitalapp.activities.NotificationsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Admin Patient List -->
        <activity
            android:name="com.amedick.hospitalapp.activities.AdminPatientListActivity"
            android:exported="false" />

        <!-- Admin Patient Details -->
        <activity
            android:name="com.amedick.hospitalapp.activities.AdminPatientDetailsActivity"
            android:exported="false" />

        <!-- Admin Doctor List -->
        <activity
            android:name="com.amedick.hospitalapp.activities.AdminDoctorListActivity"
            android:exported="false" />

        <!-- Admin Appointment List -->
        <activity
            android:name="com.amedick.hospitalapp.activities.AdminAppointmentListActivity"
            android:exported="false" />

        <!-- Admin Completed Appointments Activity -->
        <activity
            android:name="com.amedick.hospitalapp.activities.AdminCompletedAppointmentsActivity"
            android:exported="false" />

        <!-- FCM Service -->
        <service
            android:name=".notification.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
